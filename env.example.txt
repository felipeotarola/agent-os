# Required authentication settings.
ADMIN_EMAIL=you@example.com
AUTH_SECRET=replace-with-at-least-32-random-bytes

# Preferred: pbkdf2-sha256$<iterations>$<salt>$<base64url-hash>
ADMIN_PASSWORD_HASH=

# Transitional fallback only. Prefer ADMIN_PASSWORD_HASH.
ADMIN_PASSWORD=

# The existing file store used directly by agents. Do not move casually.
AGENT_OS_SECRETS_DIR=/root/.openclaw/secrets/agent-os

# Optional. When omitted, a private key is created inside the vault directory.
AGENT_OS_VAULT_FINGERPRINT_KEY=

# Self-hosted runtime.
PORT=3310
HOSTNAME=127.0.0.1
